{{ logrotate_caller_log_dir }}/*.log {
    {{ logrotate_interval }}
    rotate {{ logrotate_rotate }}
    notifempty
    missingok
    delaycompress
    create 0660 {{ logrotate_caller_user }} {{ logrotate_caller_user }}
    dateext
    dateformat {{ logrotate_dateformat }}
    copytruncate
}